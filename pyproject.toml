[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "data-detective"
version = "0.1.0"
description = "Smart Data Profiling CLI Tool"
requires-python = ">=3.8"

[project.scripts]
data-detective = "data_detective.cli:main"
